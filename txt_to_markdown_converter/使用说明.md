# TXT to Markdown Converter Skill 使用说明

## 🎯 技能简介

TXT to Markdown Converter 是一个专业的文档格式化技能，专门用于将杂乱的TXT文件转换为结构清晰、格式规范的Markdown文档。该技能采用智能识别技术，能够自动分析文档内容结构，并添加适当的Markdown格式化标记。

## ⚡ 核心特性

### 🧠 智能识别
- **标题智能检测**: 基于行长度、关键词和上下文自动识别标题
- **列表类型判断**: 区分有序列表、无序列表和中文数字列表
- **表格结构分析**: 识别制表符分隔和多空格分隔的表格
- **代码块保护**: 智能识别并保护代码块不被错误格式化

### 🎨 格式化功能
- **标题层级化**: 根据内容重要性自动分配1-6级标题
- **列表标准化**: 统一列表符号格式，转换中文数字为阿拉伯数字
- **表格规范化**: 转换为标准Markdown表格格式
- **链接可点击化**: 自动转换URL为Markdown链接格式
- **关键词高亮**: 技术术语自动添加代码标记
- **重要内容强调**: 关键词汇自动加粗显示

### 🛡️ 安全特性
- **编码自适应**: 支持UTF-8、GBK、GB2312等多种编码
- **备份机制**: 自动备份原始文件
- **代码保护**: 完整保护代码块和代码片段
- **原格式保留**: 保持原文内容不变，仅添加格式化

## 🚀 快速上手

### 环境要求
- Python 3.6+
- PyYAML库

### 安装依赖
```bash
pip install pyyaml
```

### 基本使用

#### 1. 命令行方式
```bash
# 最简单的使用方式
python txt_to_markdown_converter.py your_file.txt

# 指定输出文件名
python txt_to_markdown_converter.py input.txt -o output.md

# 原地转换（直接修改原文件）
python txt_to_markdown_converter.py input.txt --inplace

# 使用自定义配置文件
python txt_to_markdown_converter.py input.txt -c my_config.yaml

# 详细模式（显示处理过程）
python txt_to_markdown_converter.py input.txt -v
```

#### 2. Python API方式
```python
from txt_to_markdown_converter import TXTToMarkdownConverter

# 创建转换器实例
converter = TXTToMarkdownConverter()

# 转换文件
result_path = converter.convert_file("input.txt", "output.md")
print(f"转换完成: {result_path}")

# 转换文本内容
text_content = "这是标题\n这是内容"
markdown_content = converter.convert_content(text_content)
print(markdown_content)
```

## 📋 命令行详解

### 参数说明
| 参数 | 简写 | 说明 | 示例 |
|------|------|------|------|
| `input_file` | - | 输入的TXT文件路径（必需） | `document.txt` |
| `--output` | `-o` | 输出文件路径（可选） | `-o output.md` |
| `--config` | `-c` | 配置文件路径（可选） | `-c config.yaml` |
| `--inplace` | - | 原地修改文件 | `--inplace` |
| `--verbose` | `-v` | 详细输出模式 | `-v` |

### 使用示例
```bash
# 示例1: 基本转换
python txt_to_markdown_converter.py meeting_notes.txt

# 示例2: 指定输出
python txt_to_markdown_converter.py meeting_notes.txt -o formatted_meeting.md

# 示例3: 原地转换（直接修改原文件，扩展名改为.md）
python txt_to_markdown_converter.py meeting_notes.txt --inplace

# 示例4: 使用详细模式查看处理过程
python txt_to_markdown_converter.py meeting_notes.txt -v

# 示例5: 使用自定义配置
python txt_to_markdown_converter.py meeting_notes.txt -c custom_config.yaml
```

## ⚙️ 配置文件详解

### 配置文件位置
- 默认配置：内置
- 自定义配置：通过 `-c` 参数指定

### 主要配置项

#### 1. 文件处理设置
```yaml
file_handling:
  supported_extensions: [".txt", ".text", ".log", ".md"]  # 支持的文件扩展名
  backup_original: true  # 是否备份原文件
  encoding: ["utf-8", "gbk", "gb2312", "ascii"]  # 编码检测顺序
  output:
    suffix: "_formatted"  # 输出文件后缀
    extension: ".md"      # 输出文件扩展名
    keep_original: true   # 是否保留原文件
```

#### 2. 格式化规则
```yaml
formatting_rules:
  headings:
    auto_detect: true      # 是否自动检测标题
    max_level: 6          # 最大标题级别
    min_length: 2         # 标题最小长度
    max_length: 50        # 标题最大长度
    keywords:             # 标题关键词（优先识别）
      chinese: ["概述", "介绍", "总结", "结论", "功能", "特性"]
      english: ["introduction", "summary", "conclusion", "features"]

  lists:
    auto_detect: true              # 是否自动检测列表
    convert_chinese_numbers: true  # 是否转换中文数字
    unify_symbols: true           # 是否统一列表符号
    indent_spaces: 2             # 列表缩进空格数

  tables:
    auto_detect: true      # 是否自动检测表格
    separator: " | "       # 表格分隔符
    min_columns: 2         # 最小列数
    min_rows: 2           # 最小行数

  links:
    auto_format_urls: true  # 是否自动格式化URL
    add_brackets: true     # 是否添加方括号
    url_patterns:          # 支持的URL模式
      - "https?://[^\s]+"
      - "www\.[^\s]+"
      - "ftp://[^\s]+"

  emphasis:
    auto_emphasize: true   # 是否自动强调重要词汇
    important_words:       # 重要词汇列表
      chinese: ["重要", "关键", "核心", "主要", "必须", "应该", "注意"]
      english: ["important", "key", "critical", "essential", "required"]

  keywords:
    highlight_tech_terms: true  # 是否高亮技术术语
    tech_keywords:              # 技术关键词分类
      programming: ["API", "HTTP", "JSON", "XML", "HTML", "CSS", "JavaScript", "Python"]
      general: ["数据库", "算法", "函数", "变量", "类", "对象", "方法", "接口"]
```

## 📖 实际使用场景

### 场景1: 整理会议记录
**输入文件** (`meeting.txt`):
```
会议记录
今天开了项目讨论会

讨论内容
1. 项目进度汇报
2. 技术方案讨论
3. 下一步计划

技术方案
使用Python开发
后端用Django框架
前端用Vue.js

访问 https://project.example.com 查看详情
```

**转换结果** (`meeting_formatted.md`):
```markdown
# 会议记录
今天开了项目讨论会

## 讨论内容
1. 项目进度汇报
2. 技术方案讨论
3. 下一步计划

### 技术方案
使用 `Python` 开发
后端用 `Django` 框架
前端用 `Vue.js`

访问 [https://project.example.com](https://project.example.com) 查看详情
```

### 场景2: 格式化技术文档
**输入文件** (`tech_doc.txt`):
```
API接口文档
概述
本文档描述了用户管理API接口

接口列表
用户注册
用户登录
获取用户信息
更新用户资料

接口详情
POST /api/register    注册新用户
POST /api/login       用户登录
GET  /api/user/:id    获取用户信息
PUT  /api/user/:id    更新用户信息

注意事项
重要：所有接口都需要认证
必须提供有效的token
```

**转换结果** (`tech_doc_formatted.md`):
```markdown
# API接口文档
## 概述
本文档描述了用户管理API接口

## 接口列表
- 用户注册
- 用户登录
- 获取用户信息
- 更新用户资料

### 接口详情
POST /api/register | 注册新用户
POST /api/login | 用户登录
GET /api/user/:id | 获取用户信息
PUT /api/user/:id | 更新用户信息

## 注意事项
**重要**：所有接口都需要认证
**必须**提供有效的token
```

### 场景3: 处理数据导出
**输入文件** (`data_export.txt`):
```
用户数据报告
生成时间：2024-01-15

用户统计
总数    活跃用户    新用户
1000    800        50

地区分布
地区    用户数    占比
北京    300      30%
上海    250      25%
广州    200      20%
深圳    150      15%
其他    100      10%

访问 www.analytics.com 查看详细数据
```

**转换结果** (`data_export_formatted.md`):
```markdown
# 用户数据报告
生成时间：2024-01-15

## 用户统计
总数 | 活跃用户 | 新用户
1000 | 800 | 50

## 地区分布
地区 | 用户数 | 占比
北京 | 300 | 30%
上海 | 250 | 25%
广州 | 200 | 20%
深圳 | 150 | 15%
其他 | 100 | 10%

访问 [www.analytics.com](https://www.analytics.com) 查看详细数据
```

## 🧪 测试和验证

### 运行测试套件
```bash
python test_converter.py
```

### 测试覆盖内容
- ✅ 标题识别和格式化
- ✅ 列表格式化（包括中文数字转换）
- ✅ 表格检测和格式化
- ✅ 链接自动转换
- ✅ 关键词高亮
- ✅ 重要文本强调
- ✅ 代码块保护
- ✅ 文件读写操作
- ✅ 复杂文档综合处理

### 自定义测试
```python
# 创建测试内容
test_content = """
测试标题
这是测试内容
1. 第一项
2. 第二项
"""

# 转换并查看结果
converter = TXTToMarkdownConverter()
result = converter.convert_content(test_content)
print(result)
```

## 🔧 高级使用技巧

### 1. 批量处理文件
```python
import glob
from txt_to_markdown_converter import TXTToMarkdownConverter

converter = TXTToMarkdownConverter()

# 批量转换目录中的所有txt文件
for txt_file in glob.glob("*.txt"):
    md_file = txt_file.replace('.txt', '.md')
    converter.convert_file(txt_file, md_file)
    print(f"转换完成: {txt_file} -> {md_file}")
```

### 2. 自定义格式化规则
```python
# 创建自定义配置
custom_config = {
    'formatting_rules': {
        'headings': {
            'keywords': ['章节', '部分', '内容']  # 添加自定义标题关键词
        },
        'emphasis': {
            'important_words': ['关键', '重点', '核心']  # 添加自定义重要词汇
        }
    }
}

converter = TXTToMarkdownConverter()
# 更新配置
converter.config.update(custom_config)

# 使用自定义配置转换
result = converter.convert_content(your_content)
```

### 3. 集成到其他应用
```python
class DocumentProcessor:
    def __init__(self):
        self.converter = TXTToMarkdownConverter()

    def process_document(self, file_path):
        """处理单个文档"""
        try:
            output_path = self.converter.convert_file(file_path)
            return {
                'success': True,
                'output_path': output_path,
                'message': '转换成功'
            }
        except Exception as e:
            return {
                'success': False,
                'error': str(e),
                'message': '转换失败'
            }

    def batch_process(self, file_list):
        """批量处理文档"""
        results = []
        for file_path in file_list:
            result = self.process_document(file_path)
            results.append({
                'input_file': file_path,
                'result': result
            })
        return results

# 使用示例
processor = DocumentProcessor()
results = processor.batch_process(['doc1.txt', 'doc2.txt', 'doc3.txt'])
```

## ⚠️ 注意事项

### 1. 文件编码
- 默认按UTF-8、GBK、GB2312顺序尝试编码
- 如果遇到编码问题，请在配置文件中指定正确的编码
- 建议将文件保存为UTF-8编码

### 2. 备份重要文件
- 技能默认会备份原文件
- 建议在处理重要文件前手动备份
- 使用 `--inplace` 参数会直接修改原文件

### 3. 代码块保护
- 确保代码块使用正确的标记（```或~~~）
- 不完整的代码块可能影响格式化效果
- 建议在代码块前后留空行

### 4. 性能考虑
- 大文件（超过10MB）可能需要较长处理时间
- 建议先在小文件上测试效果
- 复杂表格的处理可能不够完美

## 🆘 故障排除

### 常见问题及解决方案

#### Q1: 中文字符显示乱码
**A**: 检查文件编码，确保为UTF-8，或在配置中添加正确的编码格式

#### Q2: 代码块被错误格式化
**A**: 确保代码块使用正确的开始和结束标记，并在代码块前后留空行

#### Q3: 表格识别不准确
**A**: 检查表格是否使用制表符或多个空格分隔，确保列数一致

#### Q4: 标题识别有误
**A**: 调整配置中的标题检测参数，如max_length、keywords等

#### Q5: 转换后格式不理想
**A**: 使用详细模式查看处理过程，或调整配置文件中的相关参数

## 📞 获取帮助

1. **查看帮助信息**:
   ```bash
   python txt_to_markdown_converter.py --help
   ```

2. **运行测试**:
   ```bash
   python test_converter.py
   ```

3. **查看详细日志**:
   ```bash
   python txt_to_markdown_converter.py input.txt -v
   ```

4. **参考示例文件**:
   - `example_input.txt`: 输入示例
   - `README.md`: 详细文档

---

**祝您使用愉快！享受清晰的文档格式化体验！** 🎉